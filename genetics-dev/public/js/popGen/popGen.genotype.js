//Namespaces
var popGen = popGen || {};
popGen.population = popGen.population || {};
popGen.generations = popGen.generations || {};
popGen.genotype = popGen.genotype || {};


/**
 *  Builds the modified data for genotype data. 
 *  Uses the same generations class as the allele graphing 
 *  Use the same web worker to get the frequences 
 *
 */
popGen.genotype = function() {
    //Don't think I need to do this 

}